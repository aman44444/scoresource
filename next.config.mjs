/** @type {import('next').NextConfig} */
const nextConfig = {
    // next.config.js
  
    images: {
        domains: ['cricketchampion.co.in','www.livescore.com'],
        domains: ['www.livescore.com'], // Add the domain for team images
      },
      reactStrictMode: true,
};

export default nextConfig;
